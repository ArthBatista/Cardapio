import styled from 'styled-components';

export const Container = styled.div`
  height: 4rem;

  >.Head{
    position: relative;
    font-family: 'IBM Plex Sans';
    color: #000;
    line-height: 1rem;
    left: -15%;
   text-align: center;
  }

>.mg{
  position: absolute;
  right: 0%;
  top: 0%;
}
`;
